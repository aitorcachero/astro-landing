---
import VerticalTimeline from './VerticalTimeline.astro';
import './timeline_styles.css';
import { history } from '../helpers/const.js';
---

<VerticalTimeline>
  {
    history.map((section) => {
      return <div class="border w-[200px] h-[200px]" />;
    })
  }
</VerticalTimeline>
